%%-*- mode: erlang -*-
{application, '{{appid}}',
 [ {description, "{{description}}"},
   {vsn, git},
   {registered, []},
   {applications, [
                   kernel,
                   stdlib
                  ]},
   {mod, { '{{appid}}_app', []}},
   {env, [{listen_port, 9000},
          {acceptor_num, 100}]}
  ]}.

%% vim: set filetype=erlang tabstop=4
